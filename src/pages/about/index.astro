---
import BaseLayout from "@layouts/BaseLayout.astro";

import { SITE } from "@config";

---
<BaseLayout title="关于我">
    <div class="about-wrapper">
        <h2 class="title">关于我</h2>
        <div class="description">
            <p class="p--first">
                这里是咖啡Killer的博客网站，我的一方小天地。
            </p>
			<p>
                一个喜欢荒木飞吕彦和硬核画风的阿宅、一个躲在网络角落处还喜欢碎碎念的小角色、
			    一个想努力成全栈开发大佬的菜鸟、一个总是三分钟热度，秉承着持续性混吃等死，间歇性发奋图强信条的普通人。
            <p/>
			<p>
                喜欢孤独也很享受孤独，偶尔还会发癫，平时玩游戏、看动漫、读读小说、写代码以及捣鼓各类奇怪玩意，
				有时还会去安静人少的地方逛逛，现在我正在努力让自己变的有趣。
            </p>
			<p>
                一起学习吧，愿我们变得更强
            </p>
			<p class="p--last">
                博客使用：Astro + React(TSX) + Github Action 搭建
            </p>
        </div>

        <div class="cover">
            <div>
                <img src={`${ SITE.cover }`} alt="头像_熊猫">
            </div>
            <p>熊猫头像，来自广告 <br> —— <br>"never say no to panda"</p>
		</div>

        <div class="my-links">
            <a href="//gitee.com/coffeekiller002" target="_blank"
				style="--bgcolor:#C71D231A;--brcolor:#C71D23FF;">Gitee</a>
			<a href="//github.com/CafeKiller" target="_blank"
				style="--bgcolor:#9B6EF51A;--brcolor:#9B6EF5FF;">Github</a>
			<a href="//space.bilibili.com/248826433" target="_blank"
				style="--bgcolor:#08A3D61A;--brcolor:#08A3D6FF;">Bilibili</a>
        </div>
    </div>
</BaseLayout>


<style lang="scss">
    .about-wrapper {
        padding-top: 40px;
        position: relative;
        width: 100%;
        height: 100%;
        .title {
            font-weight: 400;
            font-family: var(--lostFont);
            font-size: var(--fontsSubtit);
            line-height: 1;
            letter-spacing: 4px;
            text-indent: 4px;
            color: var(--textColor1);
        }
        .description {
            width: 360px;
            p {
                margin-bottom: 8px;
                font-size: var(--fontsSM);
                line-height: 1.6;
                color: var(--textColor3);
                &:first-child { margin-top: 16px; }
                &:last-child { 
                    font-size: var(--fontsMini); 
                    color: var(--textColor4);
                    opacity: 0.5;
                }
            }
        }
        .cover {
            position: absolute;
            top: 0;
            right: 0;
            width: 200px;
            text-align: right;
            img {
                width: 100px;
            }
            p {
                font-size: var(--fontsMini);
                color: var(--textColor4);
            }
        }
        .my-links {
			position: absolute;
			right: 0;
			bottom: 0;
			width: 166px;
			background: var(--scrapBGC);
			a {
				padding: 12px 16px;
				display: block;
				font-size: var(--fontsSM);
				letter-spacing: 1px;
                text-align: right;
				background: var(--scrapBGC);
				color: var(--textColor2);
				border-bottom: 2px solid var(--scrapBGC);
				transition: var(--baseTran);
				&:hover {
					color: var(--brcolor);
					background: var(--bgcolor);
					border-bottom: 2px solid var(--brcolor);
				}
			}
			
		}
    }

    @media screen and (max-width: 586px) {
        .about-wrapper {
            .title { text-align: center }
            .description { width: 100%; }
            .cover {
                margin-top: 40px;
                position: relative;
                text-align: left;
            }
        }
    }
</style>