---
import BaseLayout from '../layouts/BaseLayout.astro';
import PostCard from '../components/PostCard.astro'

const allPosts = await Astro.glob('./posts/*.md')
// const tags: Array<string> = [...new Set(allPosts.map((post) => post.frontmatter.tags).flat())]
// allPosts.map((post) => {
// 	// console.log(post.rawContent().replace(/\s*/g,"").slice(0,151))
// })
---

<BaseLayout title="CafeBlogs Index">
	<main>
		{
			allPosts.map((post) => {
				return (<PostCard post={post}/>)
			})
		}
	</main>
</BaseLayout>

<style>
	main {
		padding: 0 20px 60px;
	}
</style>
